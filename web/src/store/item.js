import graphqlClient from "../apollo";
import gql from "graphql-tag";
import isAuth from "../utils/isAuth";
export default {
  namespaced: true,
  state: () => ({}),
  mutations: {},
  actions: {},
  getters: {},
};
